import UnityEngine

class MaybeMonad (MonoBehaviour): 
	/*
	failure as bool = false
	
	
	$simpleMaybeMonad = function($f) {
	return $f;	
};

$SMMbind = function($left_monad, $right_monad) use($simpleMaybeMonad) {
    if( $right_monad == false){
		return false;
    }
    else{
        return $simpleMaybeMonad($left_monad);
	}
*/
	def Start ():
		pass
	
	def Update ():
		pass
